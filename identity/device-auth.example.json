{
  "_comment": "Copy this file, remove .example from the name, and replace all placeholder values with your real values from your local .openclaw directory. The operator token authenticates the local CLI against the gateway.",
  "version": 1,
  "deviceId": "YOUR_DEVICE_ID_HERE",
  "tokens": {
    "operator": {
      "token": "YOUR_OPERATOR_TOKEN_HERE",
      "role": "operator",
      "scopes": [
        "operator.admin",
        "operator.read",
        "operator.write"
      ],
      "updatedAtMs": 0
    }
  }
}
