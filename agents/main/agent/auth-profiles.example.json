{
  "_comment": "Per-provider API-key storage. Real file is gitignored. The agent loads the key from here at start-up; replace the placeholder with your NVIDIA NIM key (or another provider's key).",
  "version": 1,
  "profiles": {
    "mistralai:default": {
      "type": "api_key",
      "provider": "mistralai",
      "key": "your_nvidia_api_key"
    }
  }
}
